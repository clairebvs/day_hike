<%= @trip.name %>

Total distance : <%= @trip.total_distance %>
Average distance : <%= @trip.average_distance %>

<% @trip.trails.each do |trail| %>
  <%= trail.name %>
  <%= trail.address %>
  <%= trail.length %>
<% end %>
